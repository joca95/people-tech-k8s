apiVersion: v1
kind: Pod
metadata:
  name: memory-stress-pod
  namespace: stress-demo
spec:
  containers:
  - name: memory-stress
    image: polinux/stress
    resources:
      requests:
        cpu: "250m"
        memory: "256Mi"
      limits:
        cpu: "2"
        memory: "1Gi"
    command: ["stress"]
    args:
      - "--vm"
      - "1"
      - "--vm-bytes"
      - "256Mi"
      - "--timeout"
      - "180s"  # 3 minutos